# --- Local (développement) ---
DATABASE_URL="file:./dev.db"

# --- Production Vercel (PostgreSQL) ---
# DATABASE_URL = URL pooled (host avec « -pooler » sur Neon)
# DIRECT_URL   = URL directe (sans pooler) — obligatoire pour le build Vercel
DATABASE_URL="postgresql://user:password@ep-xxx-pooler.region.aws.neon.tech/neondb?sslmode=require"
DIRECT_URL="postgresql://user:password@ep-xxx.region.aws.neon.tech/neondb?sslmode=require"

# OpenAI
OPENAI_API_KEY=sk-votre-cle-openai

# Google Maps / Places API
# Créer une clé sur https://console.cloud.google.com/
# Activer "Places API (New)"
GOOGLE_MAPS_API_KEY=votre-cle-google-maps

# Recherche web partenaires (Serper recommandé, sinon Brave Search)
# https://serper.dev — utilisé pour fédérations, cabinets, réseaux…
# SERPER_API_KEY=votre-cle-serper
# https://brave.com/search/api/
# BRAVE_SEARCH_API_KEY=votre-cle-brave

# Resend (campagnes email)
# https://resend.com/api-keys
# Domaine vérifié Resend — utilisé comme expéditeur (FROM)
RESEND_API_KEY=re_votre-cle
RESEND_FROM="MatoFlow <contact@matoflow.fr>"
# Webhook Resend (rebonds email → liste d'appels)
# Créer un webhook sur https://resend.com/webhooks pointant vers :
# https://votre-app.vercel.app/api/webhooks/resend
# Événement : email.bounced
RESEND_WEBHOOK_SECRET=whsec_votre_secret

# URL publique de l'app déployée (suivi ouverture + désabonnement des campagnes)
# Ex. https://matoflow-prospection.vercel.app ou votre domaine custom
# NEXT_PUBLIC_APP_URL=https://votre-app.vercel.app
# Secret optionnel pour signer les liens de désabonnement (sinon NEXTAUTH_SECRET / RESEND_API_KEY)
# UNSUBSCRIBE_SECRET=
# Secret cron Vercel (généré automatiquement si vous activez les crons)
# Ce projet prévoit un cron toutes les 5 minutes pour reprendre la file d'envoi
# même sans onglet ouvert. Vérifiez que votre plan Vercel autorise cette fréquence.
# CRON_SECRET=

# --- Connexion Google (équipe) ---
# Générer le secret : openssl rand -base64 32
# NEXTAUTH_URL=https://votre-app.vercel.app
# NEXTAUTH_SECRET=votre-secret-aleatoire
# GOOGLE_CLIENT_ID=xxx.apps.googleusercontent.com
# GOOGLE_CLIENT_SECRET=xxx
# Emails administrateurs autorisés (Google OAuth)
AUTH_ALLOWED_EMAILS=gaspard.rivron2@orange.fr,contact@matoflow.fr
# Mettre false une fois Google OAuth configuré sur Vercel
AUTH_DISABLED=true
NEXT_PUBLIC_AUTH_DISABLED=true
