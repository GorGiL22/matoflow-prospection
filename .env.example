# Base de données SQLite locale
DATABASE_URL="file:./dev.db"

# OpenAI
OPENAI_API_KEY=sk-votre-cle-openai

# Google Maps / Places API
# Créer une clé sur https://console.cloud.google.com/
# Activer "Places API (New)"
GOOGLE_MAPS_API_KEY=votre-cle-google-maps

# Resend (campagnes email)
# https://resend.com/api-keys
# Domaine vérifié Resend — utilisé comme expéditeur (FROM)
RESEND_API_KEY=re_votre-cle
RESEND_FROM="MatoFlow <contact@matoflow.fr>"

# URL publique où CETTE app Next.js est déployée (suivi ouverture des campagnes).
# Peut être https://matoflow.fr si l'app y tourne avec /api/campaigns/...
# En local : laisser commenté (pas de pixel, emails sans alerte).
# NEXT_PUBLIC_APP_URL=https://matoflow.fr

# --- Connexion Google (équipe) ---
# Générer le secret : openssl rand -base64 32
# NEXTAUTH_URL=http://localhost:3000
# NEXTAUTH_SECRET=votre-secret-aleatoire
# GOOGLE_CLIENT_ID=xxx.apps.googleusercontent.com
# GOOGLE_CLIENT_SECRET=xxx
# Emails autorisés (séparés par des virgules)
# AUTH_ALLOWED_EMAILS=vous@gmail.com,collegue@gmail.com
# Mettre true en local tant que Google OAuth n'est pas configuré
AUTH_DISABLED=true
NEXT_PUBLIC_AUTH_DISABLED=true
